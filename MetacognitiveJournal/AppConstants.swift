//
//  AppConstants.swift
//  MetacognitiveJournal
//
//  Created by Paul Thomas on 4/13/25.
//


import SwiftUI

struct AppConstants {
    static let minimumEntriesForAnalysis = 3
    static let defaultPadding: CGFloat = 16
    static let cornerRadius: CGFloat = 10
    static let smallCornerRadius: CGFloat = 8
    static let chartHeight: CGFloat = 180
    static let barHeight: CGFloat = 8
    static let itemSpacing: CGFloat = 15
    static let emotionButtonSize: CGFloat = 70
    static let circleSize: CGFloat = 80
    static let barWidth: CGFloat = 30
    static let maxBarHeight: CGFloat = 200
    static let minResponseCountForAnalysis = 5
    static let maxTopKeywords = 10
    static let maxRecommendations = 5
    static let stopWords = ["the", "a", "an", "and", "or", "but", "for", "with", "in", "on", "at", "to", "from",
                        "so", "of", "by", "about", "as", "it", "was", "is", "am", "are", "be", "been",
                        "being", "have", "has", "had", "do", "does", "did", "will", "would", "should",
                        "can", "could", "may", "might", "must", "i", "you", "he", "she", "they", "we",
                        "this", "that", "these", "those", "my", "your", "his", "her", "their", "our"]
    

}
